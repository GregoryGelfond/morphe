[package]
name = "morphe-test-support"
version = "0.1.0"
description = "Shared helpers for the morphe integration suites: a Source over an inline program, and the vendored corpus classified through the trusted tier. A dev-dependency library, so its helpers are public API and no suite pays a dead-code allow for the ones it does not use."
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
morphe = { path = "../morphe" }

[lints]
workspace = true
