# path  the reading against the authority that settled it
# A member on which morphe and the pinned clingo disagree — a membership
# divergence, a canonical-rendering difference, or a ground-rule-set difference
# — recorded here with the reading that settles it as a divergence rather than a
# defect in morphe (docs/design/morphe.md §13; docs/grammar.md §11's discipline).
# An unrecorded disagreement fails the differential.
